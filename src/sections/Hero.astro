---
import IconList from '../components/IconList.astro'
import HeroeImg from 'src/assets/images/Marco02.jpg'
---

<main>
    <div class='text-icons'>
        <h1>
            Frontend Developer specialized in <span class='highlight'
                >UI/UX</span
            >
        </h1>
        <div class='description'>
            <p>
                With a solid foundation in Digital Design, I can bridge the gap
                between design and development
            </p>
            <p>
                I am currently open to work. Feel free to reach out to me at
                <a
                    href='mailto:mh94.design@gmail.com?subject=Contact from portfolio'
                >
                    mh94.design@gmail.com
                </a>
            </p>
        </div>
        <IconList />
    </div>

    <picture>
        <img src={HeroeImg.src} alt='Monitor web development' />
    </picture>
</main>

<style>
    main {
        display: grid;
        grid-template-columns: 1fr 1fr;
        align-items: center;
        gap: 50px;
    }
    .text-icons {
        display: flex;
        flex-direction: column;
        /* outline: 1px solid green; */
    }
    h1 {
        /* font-size: 40px; */
        color: var(--clr-accent);
    }
    .highlight {
        color: var(--clr-title-highlight);
    }
    .description {
        color: var(--clr-subtitle);
        margin-bottom: 32px;
    }
    a {
        color: var(--clr-title-highlight);
    }

    picture {
        container-type: inline-size;
        container-name: img;
        display: grid;
        place-content: end;
        max-height: 420px;
        max-width: 420px;
        /* background-color: red; */
    }
    img {
        /* background-color: hsla(226, 100%, 18%); */
        border: 4px solid var(--clr-accent);
        border-radius: 20%;
        /* mask-image: linear-gradient(black 80%, transparent); */
        aspect-ratio: 1 / 1;
        object-fit: contain;
    }
    @container img (width < 475px) {
    }

    @media screen and (max-width: 810px) {
        main {
            display: flex;
            gap: 20px;
            flex-direction: column;
        }
        picture {
            height: 220px;
            width: 220px;
            order: -1;
        }
        .description {
            margin-bottom: 0;
        }
    }
</style>
